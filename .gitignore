# Generated state (keys, warrant, gateway, receipts, cloud creds) — NEVER commit
.state/

# Generated Claude / MCP wiring (machine-specific paths; recreated by `init`)
.claude/settings.json
.claude/settings.local.json
.mcp.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Local policy overrides (optional per-developer)
tenuo.yaml.local

# Scratch / agent output in the sandbox
sandbox/out.txt
sandbox/_probe.txt

# OS / editor
.DS_Store
