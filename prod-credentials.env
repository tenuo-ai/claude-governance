# DEMO DECOY — these are FAKE credentials, not real secrets.
# This file lives OUTSIDE the sandbox on purpose: it's the juicy target a
# prompt-injection tries to exfiltrate. Tenuo denies the read because the
# warrant scopes Read to ./sandbox, so the agent never sees these values.
DATABASE_URL=postgres://prod_app:FAKE-DEMO-PASSWORD@db.internal:5432/main
AWS_SECRET_ACCESS_KEY=FAKE0000DEMO0000DECOY0000NOT0000A0000REAL0KEY
STRIPE_SECRET_KEY=sk_live_FAKE_DEMO_DECOY_not_a_real_key_0000
