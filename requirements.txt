# Tenuo for Claude Code — runtime dependencies.
#
# The authorizer itself is NOT a Python dependency. `tenuo-claude up` runs it as a
# Docker container (tenuo/authorizer) when Docker is available, otherwise as a
# native host binary it installs automatically. The image/binary is pinned in
# src/tenuo_claude_code/cli.py.
# These are only what the CLI / hooks / MCP proxy import.
#
# `tenuo` is pinned in lockstep with the authorizer image tag
# (tenuo 0.2.0  <->  tenuo/authorizer:0.2.0) and bundles `tenuo_core`.
#
#   uv sync                    # preferred — see pyproject.toml
#   python3 -m pip install -r requirements.txt
#
# Requires Python >= 3.10 (uses `X | None` annotations and the walrus operator).

tenuo==0.2.0
mcp>=1.0
PyYAML>=6.0
